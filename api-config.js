/* For GitHub Pages, replace this placeholder with your deployed Node backend URL. */
window.SHOPVERSE_API_URL =
  location.hostname.endsWith("github.io")
    ? "https://your-shopverse-backend-url.com"
    : "";
